{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "NgAdd",
  "title": "Add IsyFact Angular Widgets",
  "description": "Options for the ng-add schematic",
  "type": "object",
  "properties": {
    "properties": {
      "project": {
        "description": "Name of the application project that should be configured.",
        "type": "string"
      },
    "addEslint": {
      "description": "Whether to add @isyfact/eslint-plugin configuration to the project.",
      "type": "boolean",
      "default": true,
      "x-prompt": "Would you like to add IsyFact ESLint rules to your project?"
    },
    "addPrettier": {
      "description": "Whether to add @isyfact/prettier-plugin configuration to the project.",
      "type": "boolean",
      "default": true,
      "x-prompt": "Would you like to add IsyFact Prettier rules to your project?"
    }
  }
}
