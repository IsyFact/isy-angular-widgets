/**
 * Returns the visible label used by the Vitest reference component.
 * @returns Label text for the technical Vitest reference test
 */
export function buildVitestReferenceLabel(): string {
  return 'Vitest läuft';
}
